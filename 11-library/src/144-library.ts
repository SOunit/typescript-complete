import axios from 'axios';

axios.get('');

// 1. "moduleResolution": "node" in tsconfig.json

// 2. tree shaking works well in es6 by webpack

// 3. xxx.d.ts, type definition file
// - axios has .d.ts file by default
// - .d.ts is type file
// - created automatically
// "declaration": true
